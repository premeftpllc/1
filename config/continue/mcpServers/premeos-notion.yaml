name: PremeOS Notion
version: 1.0.0
schema: v1
mcpServers:
  - name: notion
    command: npx
    args:
      - "-y"
      - "@notionhq/notion-mcp-server@2.5.2"
    env:
      NOTION_TOKEN: "${{ secrets.NOTION_TOKEN }}"
