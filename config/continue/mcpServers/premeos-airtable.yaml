name: PremeOS Airtable
version: 1.0.0
schema: v1
mcpServers:
  - name: airtable
    # Airtable's hosted MCP server (no local process).
    type: streamable-http
    url: https://mcp.airtable.com/mcp
    apiKey: "${{ secrets.AIRTABLE_TOKEN }}"
